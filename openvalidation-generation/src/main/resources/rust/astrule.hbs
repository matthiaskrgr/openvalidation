
    huml.append_rule(
        "{{name}}",
        [{{tmpl "relatedfields"}}],
        {{tmpl "action"}},
        &{{#if allParentProperties}}(({{#allParentProperties}}{{tmpl "precondition"}}{{#unless @last}} and {{/unless}}{{/allParentProperties}}) and {{/if}}{{#condition}}{{tmpl}}{{/condition}}{{#if allParentProperties}})){{/if}},
        huml::RuleToggle::Enabled
    );
}